
/* This is created by Uzair Shaikh please do not misuse it. /
/ This code for security awareness webpage. */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

:root {
--bg: #07111f;
--sidebar: #0a1628;
--card: #0d1b2e;
--card2: #102238;
--border: rgba(255,255,255,.08);
--text: #f1f5f9;
--muted: #94a3b8;
--blue: #3b82f6;
--cyan: #06b6d4;
--green: #10b981;
--yellow: #f59e0b;
--red: #ef4444;
--purple: #8b5cf6;
}

{
margin: 0;
padding: 0;
box-sizing: border-box;
}
html {
scroll-behavior: smooth;
}

body {
font-family: 'Inter', sans-serif;
background: var(--bg);
color: var(--text);
min-height: 100vh;
}

button,
input {
font-family: inherit;
}

/* SIDEBAR */

.sidebar {
position: fixed;
width: 250px;
height: 100vh;
background: var(--sidebar);
border-right: 1px solid var(--border);
padding: 25px 18px;
z-index: 10;
overflow-y: auto;
}

.brand {
display: flex;
align-items: center;
gap: 12px;
padding: 5px 10px 30px;
}

.brand-icon {
width: 42px;
height: 42px;
border-radius: 12px;
background: linear-gradient(135deg, var(--blue), var(--cyan));
display: flex;
align-items: center;
justify-content: center;
font-size: 21px;
}

.brand h2 {
font-size: 18px;
}

.brand span {
color: var(--cyan);
}

.nav-title {
color: #64748b;
font-size: 11px;
font-weight: 700;
letter-spacing: 1.5px;
padding: 0 12px;
margin-bottom: 10px;
}

.nav-item {
display: flex;
align-items: center;
gap: 13px;
color: var(--muted);
padding: 12px 14px;
border-radius: 9px;
margin-bottom: 5px;
cursor: pointer;
transition: .2s;
font-size: 13px;
}

.nav-item:hover,
.nav-item.active {
background: rgba(59,130,246,.12);
color: white;
}

.nav-item.active {
border-left: 3px solid var(--blue);
}

.nav-icon {
width: 20px;
text-align: center;
}

.sidebar-bottom {
margin-top: 25px;
padding-bottom: 10px;
}

.security-status {
background: rgba(16,185,129,.08);
border: 1px solid rgba(16,185,129,.15);
padding: 14px;
border-radius: 10px;
}

.status-dot {
width: 8px;
height: 8px;
background: var(--green);
border-radius: 50%;
display: inline-block;
margin-right: 6px;
}

/* MAIN */

.main {
margin-left: 250px;
padding: 30px 38px;
max-width: 1600px;
}

.topbar {
display: flex;
justify-content: space-between;
align-items: center;
margin-bottom: 35px;
}

.page-title h1 {
font-size: 27px;
margin-bottom: 5px;
}

.page-title p {
color: var(--muted);
font-size: 13px;
}

.top-right {
display: flex;
align-items: center;
gap: 15px;
}

.sim-badge {
padding: 9px 14px;
border-radius: 20px;
background: rgba(59,130,246,.1);
border: 1px solid rgba(59,130,246,.2);
color: #93c5fd;
font-size: 12px;
}

.avatar {
width: 38px;
height: 38px;
border-radius: 50%;
background: linear-gradient(135deg, #6366f1, #8b5cf6);
display: flex;
align-items: center;
justify-content: center;
font-weight: 700;
}

/* HERO */

.hero {
background:
radial-gradient(circle at 85% 30%, rgba(6,182,212,.14), transparent 30%),
radial-gradient(circle at 60% 80%, rgba(59,130,246,.12), transparent 30%),
var(--card);
border: 1px solid var(--border);
border-radius: 18px;
padding: 35px;
margin-bottom: 25px;
position: relative;
overflow: hidden;
}

.hero::after {
content: "◈";
position: absolute;
right: 50px;
top: 15px;
font-size: 180px;
color: rgba(59,130,246,.035);
}

.hero-content {
position: relative;
z-index: 1;
max-width: 700px;
}

.eyebrow {
color: var(--cyan);
text-transform: uppercase;
letter-spacing: 2px;
font-size: 11px;
font-weight: 700;
margin-bottom: 12px;
}

.hero h2 {
font-size: 32px;
line-height: 1.2;
margin-bottom: 13px;
}

.hero p {
color: var(--muted);
font-size: 14px;
max-width: 650px;
}

.primary-btn {
border: none;
margin-top: 24px;
padding: 13px 21px;
border-radius: 9px;
background: linear-gradient(135deg, var(--blue), #2563eb);
color: white;
font-weight: 600;
cursor: pointer;
transition: .2s;
box-shadow: 0 8px 25px rgba(37,99,235,.2);
}

.primary-btn:hover {
transform: translateY(-2px);
box-shadow: 0 12px 30px rgba(37,99,235,.3);
}

/* STATS */

.stats {
display: grid;
grid-template-columns: repeat(4, 1fr);
gap: 16px;
margin-bottom: 25px;
}

.stat-card {
background: var(--card);
border: 1px solid var(--border);
border-radius: 14px;
padding: 20px;
}

.stat-top {
display: flex;
justify-content: space-between;
color: var(--muted);
font-size: 12px;
}

.stat-icon {
width: 35px;
height: 35px;
border-radius: 8px;
display: flex;
align-items: center;
justify-content: center;
background: rgba(59,130,246,.1);
color: #60a5fa;
}

.stat-number {
font-size: 27px;
font-weight: 800;
margin-top: 14px;
}

.stat-change {
font-size: 11px;
color: var(--green);
margin-top: 4px;
}

/* GRID */

.grid {
display: grid;
grid-template-columns: 1.5fr 1fr;
gap: 20px;
}

.card {
background: var(--card);
border: 1px solid var(--border);
border-radius: 15px;
padding: 23px;
margin-bottom: 20px;
}

.card-header {
display: flex;
justify-content: space-between;
align-items: center;
margin-bottom: 20px;
}

.card-title {
font-size: 16px;
font-weight: 700;
}

.card-subtitle {
color: var(--muted);
font-size: 12px;
margin-top: 3px;
}

/* EMAIL */

.email-window {
background: #f8fafc;
color: #172033;
border-radius: 11px;
overflow: hidden;
}

.email-toolbar {
height: 40px;
background: #e2e8f0;
display: flex;
align-items: center;
gap: 7px;
padding-left: 15px;
}

.window-dot {
width: 9px;
height: 9px;
border-radius: 50%;
}

.dot-red { background: #ef4444; }
.dot-yellow { background: #f59e0b; }
.dot-green { background: #10b981; }

.email-meta {
padding: 18px;
border-bottom: 1px solid #e2e8f0;
}

.email-row {
display: flex;
margin-bottom: 7px;
font-size: 12px;
}

.email-row strong {
width: 70px;
color: #64748b;
}

.email-body {
padding: 25px;
min-height: 270px;
font-size: 13px;
}

.email-body p {
margin-bottom: 15px;
}

.fake-link {
display: inline-block;
padding: 11px 18px;
background: #2563eb;
color: white;
border-radius: 6px;
font-weight: 600;
cursor: pointer;
margin: 8px 0;
border: none;
}

.fake-link:hover {
background: #1d4ed8;
}

.email-warning {
margin-top: 15px;
padding: 12px;
background: #fff7ed;
border-left: 4px solid #f59e0b;
color: #92400e;
font-size: 11px;
}

/* RED FLAGS */

.flag {
display: flex;
gap: 13px;
padding: 13px 0;
border-bottom: 1px solid var(--border);
}

.flag:last-child {
border-bottom: none;
}

.flag-icon {
width: 30px;
height: 30px;
border-radius: 7px;
background: rgba(239,68,68,.1);
color: #f87171;
display: flex;
align-items: center;
justify-content: center;
flex-shrink: 0;
}

.flag strong {
display: block;
font-size: 12px;
margin-bottom: 3px;
}

.flag p {
color: var(--muted);
font-size: 11px;
}

/* PROGRESS */

.progress-box {
margin-top: 15px;
}

.progress-label {
display: flex;
justify-content: space-between;
font-size: 12px;
margin-bottom: 7px;
}

.progress {
height: 7px;
background: #1e293b;
border-radius: 10px;
overflow: hidden;
}

.progress-fill {
height: 100%;
background: linear-gradient(90deg, var(--blue), var(--cyan));
border-radius: 10px;
transition: width .5s ease;
}

/* EDUCATION */

.education {
display: none;
background:
linear-gradient(135deg, rgba(16,185,129,.08), transparent),
var(--card);
border-color: rgba(16,185,129,.2);
}

.lesson {
display: grid;
grid-template-columns: repeat(3,1fr);
gap: 12px;
}

.lesson-item {
background: rgba(255,255,255,.025);
border: 1px solid var(--border);
padding: 17px;
border-radius: 10px;
}

.lesson-item span {
font-size: 20px;
}

.lesson-item h4 {
font-size: 12px;
margin: 8px 0 5px;
}

.lesson-item p {
color: var(--muted);
font-size: 10px;
line-height: 1.5;
}

/* QUIZ */

.quiz-option {
width: 100%;
text-align: left;
padding: 12px;
margin-bottom: 8px;
background: #101f34;
color: white;
border: 1px solid var(--border);
border-radius: 8px;
cursor: pointer;
font-size: 12px;
}

.quiz-option:hover {
border-color: var(--blue);
background: rgba(59,130,246,.08);
}

#quizResult {
margin-top: 15px;
font-size: 12px;
}

/* CONSENT */

.consent {
background: rgba(245,158,11,.05);
border: 1px solid rgba(245,158,11,.15);
border-radius: 10px;
padding: 15px;
margin-bottom: 20px;
}

.consent label {
display: flex;
gap: 10px;
color: var(--muted);
font-size: 11px;
}

.consent input {
accent-color: var(--blue);
}

/* REPORTS */

.report-grid {
display: grid;
grid-template-columns: repeat(3, 1fr);
gap: 12px;
}

.report-item {
padding: 18px;
background: rgba(255,255,255,.025);
border: 1px solid var(--border);
border-radius: 10px;
}

.report-item .report-number {
font-size: 25px;
font-weight: 800;
margin-bottom: 5px;
}

.report-item p {
color: var(--muted);
font-size: 11px;
}

/* SETTINGS */

.setting-row {
display: flex;
justify-content: space-between;
align-items: center;
padding: 15px 0;
border-bottom: 1px solid var(--border);
}

.setting-row:last-child {
border-bottom: none;
}

.setting-row strong {
font-size: 12px;
}

.setting-row p {
color: var(--muted);
font-size: 10px;
margin-top: 4px;
}

.toggle {
width: 42px;
height: 22px;
background: var(--blue);
border-radius: 20px;
position: relative;
cursor: pointer;
}

.toggle::after {
content: "";
position: absolute;
width: 16px;
height: 16px;
background: white;
border-radius: 50%;
right: 3px;
top: 3px;
}

/* MODAL */

.modal {
position: fixed;
inset: 0;
background: rgba(2,6,23,.82);
backdrop-filter: blur(7px);
display: none;
align-items: center;
justify-content: center;
z-index: 100;
padding: 20px;
}

.modal-box {
width: 430px;
background: #f8fafc;
color: #172033;
border-radius: 15px;
padding: 32px;
box-shadow: 0 30px 80px rgba(0,0,0,.4);
}

.modal-logo {
text-align: center;
font-size: 22px;
font-weight: 800;
color: #2563eb;
margin-bottom: 8px;
}

.demo-label {
text-align: center;
color: #64748b;
font-size: 11px;
margin-bottom: 25px;
}

.form-group {
margin-bottom: 15px;
}

.form-group label {
display: block;
font-size: 12px;
font-weight: 600;
margin-bottom: 6px;
}

.form-group input {
width: 100%;
border: 1px solid #cbd5e1;
border-radius: 7px;
padding: 12px;
outline: none;
}

.form-group input:focus {
border-color: #2563eb;
}

.login-btn {
width: 100%;
padding: 12px;
border: none;
border-radius: 7px;
background: #2563eb;
color: white;
font-weight: 600;
cursor: pointer;
}

.safe-notice {
background: #eff6ff;
color: #1d4ed8;
padding: 10px;
border-radius: 7px;
font-size: 10px;
margin-top: 15px;
}

/* FOOTER */

footer {
color: #64748b;
text-align: center;
padding: 25px;
font-size: 11px;
}

/* RESPONSIVE */

@media(max-width: 1100px) {

.stats {
    grid-template-columns: repeat(2,1fr);
}

.grid {
    grid-template-columns: 1fr;
}

.report-grid {
    grid-template-columns: repeat(2,1fr);
}
}

@media(max-width: 750px) {

.sidebar {
    display: none;
}

.main {
    margin-left: 0;
    padding: 20px;
}

.stats {
    grid-template-columns: 1fr;
}

.hero h2 {
    font-size: 25px;
}

.lesson,
.report-grid {
    grid-template-columns: 1fr;
}

.top-right .sim-badge {
    display: none;
}
}
